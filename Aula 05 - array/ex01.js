// Criando a funçao
function array(nome, idade, curso) {
    // Criando o array e adicionando os parametros no array
    let aluno = [nome, idade, curso];
    // Exibindo o array no console
    console.log(aluno);
}

// Chamando a função
array("carlos", 31, "DEV");